# Current Routing
Date: 2026-05-26
Use this file as the short current route before opening the append-only docs.
## New Session Start
- Read this file first, then the top of `docs/SESSION_HANDOFF.md`, then
  `docs/CURRENT_SYSTEM_STATE.md`.
- Open
  `docs/LOLO_TYLERS_KITCHEN_SOURCE_SET_CONTRACT_BLOCKER_MILESTONE_PLAN.md`
  when continuing implementation on the remaining tracked Lolo review-local
  source-set contract blocker for the ECID preliminary historical lane. Open
  `docs/LOLO_TYLERS_KITCHEN_REPLACEMENT_FEASIBILITY_BLOCKER_MILESTONE_PLAN.md`
  only as the exact predecessor that reduced the generic replacement-feasibility
  lane into this narrower packet. Open
  `docs/ECID_PRELIMINARY_HISTORICAL_REBASELINE_BLOCKER_MILESTONE_PLAN.md`
  only as the older ECID predecessor closeout that named the Lolo
  replacement-feasibility lane. Open
  `docs/ECID_PRELIMINARY_HISTORICAL_LANE_RESOLUTION_MILESTONE_PLAN.md` only as
  the blocked parent record for the fail-closed slot gate and stop-condition
  evidence. Open
  `docs/LOLO_TYLERS_KITCHEN_EXAMPLE_PACKAGE_MILESTONE_PLAN.md` only as the
  broader package-authority and forest-registry parent record for Tyler's
  Kitchen. Open
  `docs/REAL_PACKAGE_REVIEW_REPLAY_REPAIR_MILESTONE_PLAN.md` only as the
  resolved predecessor packet when you need its historical closeout context.
## Active Route
- Active packet:
  `docs/LOLO_TYLERS_KITCHEN_SOURCE_SET_CONTRACT_BLOCKER_MILESTONE_PLAN.md`
- Live next slice:
  replacement-feasibility Milestone 1 is now reduced locally: fresh tracked
  Lolo evidence shows that the replay context and tracked `v1-ea-eval`
  contract still point at `source-set-4fb59e9eb43045cb`, the live
  `v1_ea_eval_results.json` now reports
  `source-set-5e65d845ce77e1a0` and fails
  `review_identity_matches_contract`, most live review-local outputs now bind
  `5e65...`, `generated_rule_pack_validation.json` and review
  `phase_eval_results.json` still bind `4fb...`, and
  `downstream_direct_evaluation` also sees stale compliance direct-eval on
  `source-set-f70ea11e04ae3d53`. Continue in Milestone 1 of the new source-set
  contract blocker to classify that contract chain before any roster, slot, or
  ready-state changes. Do not flip the ECID historical slot to `ready`, admit
  Lolo into the governed roster, or reopen
  `docs/REAL_PACKAGE_REVIEW_REPLAY_REPAIR_MILESTONE_PLAN.md`
- Aggregate truth:
  ECID current promotion and South Plateau reviewer-ready expansion are
  green; non-strict `promotion-suite` is `current_promotion_ready=true` and
  `promotion_ready=true`; strict expansion now fails only because the ECID
  preliminary-EA historical slot is truthfully `selected_not_ready` with
  `failure_category="historical_source_set_split"` (`1` open expansion slot,
  `0` open expansion artifacts`). Fresh blocker proving also now shows that
  the old `ba8...` closure assumption is stale under current artifacts,
  `4fb...` remains source-set `phase-eval` red, and the tracked Lolo
  candidate now splits across tracked `4fb...` config surfaces, live
  `5e65...` review-local artifacts, and stale `f70...` downstream direct-eval
  coverage. The ECID blocker, the Lolo replacement-feasibility packet, and
  the broader Lolo example packet are now historical lineage only; live work
  begins in the exact Lolo source-set contract blocker
- Document-routing entrypoint: `docs/AGENT_START_HERE.md`
- Live architecture state:
  `docs/CURRENT_SYSTEM_STATE.md`, `docs/SESSION_HANDOFF.md`,
  `docs/ARCHITECTURE.md`, and `config/architecture_large_file_inventory_v1.json`
## Deep Reads
- Core: `docs/CURRENT_SYSTEM_STATE.md`, `docs/SESSION_HANDOFF.md`,
  `docs/ARCHITECTURE.md`
- Follow-ons:
  `docs/LOLO_TYLERS_KITCHEN_SOURCE_SET_CONTRACT_BLOCKER_MILESTONE_PLAN.md`,
  `docs/LOLO_TYLERS_KITCHEN_REPLACEMENT_FEASIBILITY_BLOCKER_MILESTONE_PLAN.md`,
  `docs/ECID_PRELIMINARY_HISTORICAL_REBASELINE_BLOCKER_MILESTONE_PLAN.md`,
  `docs/LOLO_TYLERS_KITCHEN_EXAMPLE_PACKAGE_MILESTONE_PLAN.md`,
  `docs/ECID_PRELIMINARY_HISTORICAL_LANE_RESOLUTION_MILESTONE_PLAN.md`,
  `docs/ARCHITECTURE_GOVERNANCE_REBASELINE_MILESTONE_PLAN.md`,
  `docs/UNDER_800_HOTSPOT_REDUCTION_MILESTONE_PLAN.md`,
  `docs/FULL_CANONICAL_DIRECT_FILE_CAPTURE_QUEUE_RESOLUTION_MILESTONE_PLAN.md`,
  `docs/REAL_PACKAGE_REVIEW_REPLAY_REPAIR_MILESTONE_PLAN.md`
